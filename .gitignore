# Full git clones live locally; re-clone via scripts/clone-sources.sh
sources/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Fuzz build artifacts / corpus (keep source harness + logs summaries)
fuzz/**/hfuzz_workspace/
fuzz/**/hfuzz_target/
fuzz/**/target/
fuzz/**/onchain_v1.so
sources/spl-token-swap-legacy/
